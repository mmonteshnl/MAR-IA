"use client";

import { GeneralConfigManager } from '@/components/config/GeneralConfigManager';

export default function ConfigPage() {
  return (
    <div className="container mx-auto py-6">
      <GeneralConfigManager />
    </div>
  );
}